#include "holberton.h"
/**
 *factorial - print with recurssion.
 *@n: value to be evaluate.
 *Return: int.
 */
int factorial(int n)
{
int sum;
int x;
sum = 1;
x = 1;
while (x <= n)
{
sum = sum * x;
x++;
}
return (sum);
}
