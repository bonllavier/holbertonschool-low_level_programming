#include "dog.h"
#include<stdio.h>
#include <stdlib.h>
/**
 *string_nconcat - return 2d array
 *@s1: string to copy.
 *@s2: height.
 *@n: size.
 *Return: int.
 */
void init_dog(struct dog *d, char *name, float age, char *owner)
{
  (*d).name = name;
  (*d).age = age;
  (*d).owner = owner;
}
