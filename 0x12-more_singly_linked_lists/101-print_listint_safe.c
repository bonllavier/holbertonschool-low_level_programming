#include<stdio.h>
#include<stdlib.h>
#include <string.h>
#include "lists.h"
#include <stddef.h>
/**
 *print_listint_safe - safe
 *@head: head
 *Return: address.
 */
size_t print_listint_safe(const listint_t *head)
{
	size_t h = 0;

	if (head == NULL)
	{
		exit(98);
	}
	return (h);
}
