#include<stdio.h>
#include<stdlib.h>
#include <string.h>
#include "lists.h"
/**
 *add_nodeint_end - put new head
 *@head: head
 *@n: param.
 *Return: address.
 */
size_t free_listint_safe(listint_t **h)
{
	size_t h = 0;

	if (head == NULL)
	{
		return;
	}
	return (h);
}
