#include "holberton.h"
#include <stdio.h>
/**
 * add - function that adds two integers and returns the result
 * @x: number to print last digit of
 * @y: number to print last digit of
 * Return: the value of last digit
 */
int add(int x, int y)
{
return (x + y);
}
