#include<stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
 *main - adds pos.
 *@argc: value to be evaluate.
 *@argv: value to be evaluate.
 *Return: 0.
 */
void free_grid(int **grid, int height)
{
  int i;
  for (i = 0; i < height ; i++)
    {
      free(grid[i]);
    }
  free(grid);
}
